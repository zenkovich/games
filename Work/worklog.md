# Word Fall — worklog

## 2026-08-14 — большой рефакторинг: модульная архитектура, данные на C++

Гигантский JS-скрипт и «сделано как попало» разобраны на слои. Полная схема —
`Work/Architecture.md`.

### Данные → C++ (Sources/Game/WordFall/Core/)
- `WordBoard` (поле/мешок/выбор/очки/гравитация/пауэрапы, `WordMoveResult` для вью),
  `WordLevel` (задачи/ходы/заряды/победа), `WordDictionary` (23.5k слов, WString),
  `PlayerProgress` (текущий уровень + рекорды, Save/Load JSON), `WordFallConfigs`
  (сериализуемые конфиги буквы/задачи/уровня/поля + кампания по умолчанию).
- `WordFallGameService` — сервисная нода GameService в bootstrap-сцене: владеет
  ядром; конфиги кампании и поля — @SERIALIZABLE @EDITOR_PROPERTY (редактируются в
  редакторе); SCRIPTABLE-фасад для JS (~30 методов, составные ответы — ScriptValue);
  ревизия состояния для pull-синка вьюх; сохранение прогресса при переходе уровня.
- Мост JS↔C++ подтверждён тестом: `actor.GetComponent("WordFallGameService")` +
  @SCRIPTABLE методы + ScriptValue-объекты/массивы работают из QuickJS.

### Отображение
- Bootstrap строит якорную иерархию виджетов (`WidgetLayout` anchors: полосы к
  верху/низу экрана, поле по центру, задачи к левому краю); секции — виджеты,
  дети позиционируются якорями внутри них.
- Прототипы `Tile/WordSlot/Booster` (.proto в Assets/WordFall/Prototypes) —
  строятся билдерами `WordFallUiFactory` и сохраняются как ассеты. ⚠ Рантайм
  инстанцирует билдерами: повторный клон Widget-актора из ActorAsset в одном
  процессе даёт пустые инстансы (нужно расследование в движке — багрепорт).
- JS-вьюхи (6 модулей на ScriptableComponent, по одной на секцию UI): клики,
  синк по ревизии сервиса, перелёт буквы (безье), падение плиток. Зависимости
  (`serviceActor`, `vfxActor`) инжектятся бутстрапом в инстансы; публичные поля
  вьюх (скорости, геометрия) видны в редакторе.
- VFX: `WordFallVfx` — пул burst-эмиттеров частиц (искры сгорания, взрыв, салют
  победы), спрайт `vfx_spark.png` (Gemini). JS зовёт PlayBurn/PlayExplosion/PlayWin.
- Вдавливание кнопок — как раньше (мультитрек-клип в стейте pressed), перенесено
  в фабрику.

### Грабли
- CodeTool перегенерирует META при сборке: поле `type` конфликтует с макросом
  SERIALIZABLE (→ `taskType`), `unsigned int` парсится криво (→ `int`), строковые
  дефолты полей должны быть `String("...")`.
- Багфикс движка: `DataDocument::SaveToFile` возвращал false при успехе.
- `Asset::Save` ждёт путь ОТНОСИТЕЛЬНО Assets (базу подставляет дерево ассетов);
  с полным путём молча не пишет. Папку под ассет надо создавать самому.
- Виджет со стейтом `visible`, выключенный до первого апдейта, остаётся видимым
  (hide-анимация не проигрывается) — попап переведён на мгновенное скрытие.
- JS-классы вьюх — присваиванием в глобал (`X = class X ...`): `class X {}` не
  создаёт own property globalThis, ScriptableComponent класс не находит.

### Порт хореографии хода (из восстановленного файла)
- `WordFallFxView.js` — секвенсор эффектов на секции Fx (пулы: 8 летящих цифр со
  свечениями, итог, 10 вспышек, 10 звёзд, 2 луча): подтверждающие вспышки на буквах,
  полёт цифр в счётчик «+N» с накоплением, пауза, полёт итога в прогресс-бар с
  плавным дозаполнением; эффекты пауэрапов (вспышка бомбы с отголосками, лучи
  ракеты крестом, звёзды палочки по активированным буквам).
- Отложенный обвал: BoardView держит поле до конца подтверждения
  (HoldCollapse/ApplyPendingCollapse; ревизионный синк доски приостановлен),
  HudView ведёт экранный счёт (`_displayScore`) с твином, PopupView ждёт конца
  секвенции (fx.IsBusy). Любое новое действие мгновенно финализирует (fx.Finish).
- В `WordMoveResult` добавлены `powerupsUsed` и `destroyed` — для эффектов.

### Инцидент и восстановление
- При рефакторинге удалил рабочую копию `WordFallGame.js`, не заметив, что она
  ушла вперёд закоммиченной (незакоммиченные правки контрибьютора: словарь 23.5k,
  процедурные 100 уровней, задачи anyWords/wordScore, разброс сида, страховка
  выполнимости, быстрый hint, новый анти-клин). Восстановил из
  `BuiltAssets/Android/Data/Scripts/WordFallGame.js` (сборка ассетов не удаляет
  файлы) — копия лежит в `Work/Recovered_WordFallGame.js`. Все механики
  портированы в C++ ядро (`WordFallLevels`, задачи, страховка, hint с отсевом,
  анти-клин по 8-соседям, разброс сида, 30 подсказок). Урок: перед удалением
  трекнутых файлов сверять рабочую копию с HEAD.

### Тесты и сборки
- 36 C++ тестов ядра (headless, без JS-eval) + 2 моста + 10 UI-тестов — все зелёные
  (включая: валидность и детерминизм 100 процедурных уровней, anyWords/wordScore,
  подсев страховки, разброс сида).
- Bootstrap-сцена регенерируется тестом (`WordFall.scn` = Bootstrap + GameService).
- Сборки: mac ✓, wasm ✓ (Game.html), android ✓, ios — код компилируется (ios-sim ✓),
  device-сборка требует подписи (team_id), как и было.

## 2026-08-11 — доводка хореографии хода и скрытый сид

- **Отложенное исчезновение букв**: по принятию слова плитки остаются на месте —
  играют вспышки подтверждения (стаггер 0.06с), цифры стартуют по очереди, и лишь
  после запуска всех цифр (`_pendingCollapse`, ~0.2с + 0.1с/буква) поле обваливается
  и сверху сыплются новые буквы. Финализация секвенции применяет обвал мгновенно.
- **Полёт цифр**: под каждой цифрой — свечение (`FxGlow0..7`, разгорается и тает к
  цели), цель смещена левее (влёт в счётчик), каждый прилёт бьёт маленькой
  вспышкой-влётом; пул вспышек расширен до 10.
- **Сид слова-задания** больше не выкладывается в линию посередине — буквы
  разбрасываются по случайным клеткам поля: слово гарантированно собираемо
  (свободный выбор — важен только набор букв), но не угадывается с одного взгляда.
- Тесты: скрин подтверждения (03: буквы с рамками и вспышкой ещё на месте), скрин
  полёта цифр со свечением (10), проверка вью-слоя бомбы ждёт отложенный обвал.
  Все 7 сьютов зелёные.

## 2026-08-11 — анимации начисления очков и эффекты бонусов

- **Секвенсор** в вью-контроллере (`_Fx(delay, dur, apply, done)` + `_UpdateFx`):
  лёгкие тайм-лайновые анимации со smoothstep и паузами.
- **Начисление очков этапами**: при сгорании букв их номиналы («+2», «+1»...) летят
  по дугам с плиток в счётчик у панели слова (стаггер 0.08с), сумма накапливается по
  мере прилёта, затем показывается полная сумма с бонусами пауэрапов, пауза ~0.75с,
  итог летит в прогресс-бар — и только тогда обновляются цифры счёта и заполнение
  бара (плавное, 0.35с). Экранный счёт `_displayScore` намеренно отстаёт от модели;
  попап победы/поражения ждёт конца последовательности. Новый ход/рестарт мгновенно
  финализируют текущую анимацию.
- **Эффекты пауэрапов**: бомба — большая вспышка + мелкие отголоски на взорванных
  клетках (в результат хода добавлено поле `destroyed`); ракета — лучи, растущие
  крестом из плитки (спрайт заливки бара), с задержкой и растворением; палочка —
  вспышка и звёзды на каждой активированной букве со стаггером. Фейды — через
  scriptable-прозрачность слоя.
- **Арт эффектов** сгенерирован Gemini: `ui_fx_flash` (радиальная вспышка),
  `ui_fx_star` (четырёхлучевая звезда); в bootstrap — пулы Fx-виджетов
  (8 цифр, итог, 4 вспышки, 10 звёзд, 2 луча).
- Попутно: панели HUD/слова подняты по глубине выше плиток — спавн-плитки при
  падении ныряют за панели, а не пролетают поверх.
- Тесты: WinShowsPopup проверяет отложенный попап (win зафиксирован сразу, попап —
  после анимации); новый BombActivationPlaysEffects (реальный клик-прогон эффекта,
  скрин 10_bomb_fx.png, очистка секвенсора). Все 7 сьютов зелёные.

## 2026-08-11 — свободный выбор, 100 процедурных уровней, страховка заданий

- **Механика выбора** возвращена к свободной (любые плитки в любом порядке); вернулись
  кластерный бонус (max связная компонента по 8-соседству) и множитель длины 1+0.25(L−3).
  Подсказка снова подбирает мультинабор (жадно, дорогие первыми), а не путь.
- **Кампания — 100 уровней**, генерируются детерминированно по индексу
  (`WordFallLevels.Get`): рампа цели (≈250 + 50/10 уровней), ходов (12–16) и льда (3–10);
  задания (3–5) из пула. Слова-задания выбираются из словаря (3–5 букв, только частые
  буквы — обиходная лексика); первое всегда сидится на поле.
- **Новые типы заданий**: `anyWords` (собрать N любых слов) и `wordScore` (слово на X+
  очков) — вдобавок к word/length/powerup/clearIce. При clearIce льда не больше 6.
- **Страховка выполнимости**: после каждого хода/бустера проверяется достижимость
  каждого незакрытого задания; невыполнимое чинится подсевом недостающих букв в
  случайные клетки (без льда и пауэрапов). При свободном выборе собираемость ==
  мультинабор букв → проверки точные и дешёвые (`_AnyWordExists` — первый матч
  префильтра; глобальный кэш счётчиков букв слова `WordDictNeeds`).
- **Бот**: стратегия — молотки как добивание льда (льдин ≤ зарядов или ходы на исходе),
  сборка слов-заданий напрямую, пауэрап-задачи словом точной длины, length-задачи,
  слова около льда для clearIce, иначе самое дорогое слово. Итераций калибровки было
  три: сначала бот проигрывал 64/100 по word2/clearIce-задачам (стратегия), потом
  21/100 по powerup (слова точной длины), после — 2/100 по clearIce (жадные молотки).
  Финально: **300/300 побед** (100 уровней × 3 набора сидов, node).
- Тесты: пересчитан скоринг, разбросанный выбор снова валиден; новые тесты anyWords/
  wordScore/ремонта/валидности и детерминизма генератора; BotCanWinGeneratedLevels
  (уровни 1/50/100 в headless — полный охват node-прогоном). Сюита ужалась со 102 до
  16 секунд (лёгкие проверки достижимости вместо полного перебора).

## 2026-08-10 — диагонали возвращены

- По решению контрибьютора выбор по диагонали снова разрешён (8-соседство в
  `ToggleSelect`, DFS подсказки и анти-клине спавна). Тест снова DiagonalChainAllowed.
- Цели уровней возвращены к диагональной калибровке **180/250/350** (на орто-целях
  140/190/260 уровни стали бы тривиальными). Бот: 15/15 побед
  (181–223 / 250–288 / 359–515), все сьюты зелёные.

## 2026-08-10 — подсказок больше

- Заряды подсказки в уровнях кампании подняты с 3 до 30 (остальные бустеры — по 3;
  тестовая песочница `WordFallConfig.level` не тронута — на её заряды завязаны тесты).

## 2026-08-10 (поздняя ночь, дополнение) — цепочки только орто

- Диагонали убраны из механики: сосед цепочки — только по горизонтали/вертикали
  (|dc|+|dr| == 1). Затронуто: `ToggleSelect` (диагональный клик = `restart`),
  DFS подсказки (4 направления), анти-клин спавна (гласная в орто-соседстве).
- Тест DiagonalChainAllowed перевёрнут в DiagonalTileRestartsSelection.
- Ребаланс по боту: орто-цепочки приносят меньше (бот: 180–204 / 239–258 / 314–373
  против прежних целей 180/250/350 — впритык и проигрыши) → цели уровней снижены
  до **140/190/260**. Бот выигрывает 15/15 прогонов (5 сидов × 3 уровня), все сьюты
  зелёные.

## 2026-08-10 (поздняя ночь) — механика цепочек

Смена кор-механики: слово набирается **цепочкой** — первая буква любая, каждая
следующая обязана быть соседом предыдущей по 8 направлениям; случайно разбросанные
буквы больше не собрать.

- **Выбор** (`ToggleSelect`): проверка соседства с последней буквой; клик по дальней
  плитке — `restart`: начинает новое слово с неё (вью гасит перелёты и запускает
  флаер в нулевой слот).
- **Скоринг**: кластерный бонус потерял смысл (цепочка всегда связна) — удалён;
  вместо него усиленный множитель длины 1+0.5(L−3): 4 буквы ×1.5, 5 ×2 … 8 ×3.5.
- **Спавн**: анти-клин переведён с вертикального правила на соседское — если среди
  заполненных 8-соседей клетки ≥2 букв и нет ни одной гласной, тянется гласная;
  у любой согласной есть гласная рядом → цепочки остаются собираемыми.
- **Подсказка**: `FindBestWord` ищет не мультинабор букв, а **путь-цепочку** (DFS с
  visited, джокер матчит любую букву; старт из клеток первой буквы). Отсев по
  счётчикам букв сохранён. Добавлен фильтр длины `FindBestWord(len)` — для
  length-задач. Пауэрапы/бустеры совместимы с механикой без изменений.
- **Баланс**: цели уровней подобраны по боту — 180/250/350 (бот с идеальной
  подсказкой набирал 253–290 / 351–406 / 466–534 при старых 250/350/500 — впритык;
  человеку нужен запас).
- **Тесты**: переписаны под цепочку и новый скоринг (FarTileRestartsSelection,
  DiagonalChainAllowed, ChainScoreUsesLengthMultiplier и др.); главный новый —
  **BotCanWinEveryCampaignLevel**: бот (первым ходом посеянное слово задания → молоток
  по льду без траты хода → length-задачи словами нужной длины → иначе самое дорогое
  слово-цепочка) выигрывает все 3 уровня на сидах 42/7/2026; в node прогнано 5 сидов
  на уровень — 15/15 побед. Ложная граница в тесте подсказки исправлена (база ФАКТ
  9, а не 11). Все сьюты зелёные.

## 2026-08-10 (ночь) — полноценный словарь

- Встроенный словарь (~470 слов) заменён корпусом **Harrix/Russian-Nouns** (MIT,
  на базе OpenCorpora): 51 300 существительных в начальной форме →
  после фильтров **23 509 слов**. Сырец: `Work/Dict/russian_nouns.txt`, генератор:
  `Work/Dict/make_dict.py` — фильтры: 3–8 букв, только кириллица, «ё»→«е», без «ъ»
  (нет такой плитки в мешке); словарь вписывается в WordFallGame.js одной строкой
  (сплит при загрузке — парсится быстрее массива литералов, файл ~370 КБ).
- Обсценной лексики в корпусе нет (проверено грепом по корням); первый вариант
  substring-бан-листа ловил только ложные срабатывания («команда» из-за «манда»,
  «алебарда», «скипидар») — оставлен точный бан-лист на случай смены корпуса.
- Подсказка оптимизирована под большой словарь: перед жадным подбором плиток слово
  отсеивается по счётчикам букв поля (дефицит букв > числа джокеров — мимо);
  вся модельная сюита с 23.5k словарём — под секунду.
- Тест подсказки перестал ждать ровно «ФАКТ» (теперь находятся слова дороже,
  например КОФТА) — проверяется нижняя граница стоимости и принимаемость выбора.
- Все сьюты зелёные.

## 2026-08-10 (ночь, третья итерация) — задник панели ввода

- Экстракт задника панели ввода давал грубую пере-нарисованную тень выемки. Переделано:
  кроп бара из концепта → `edit_image` («убрать плашки и кнопку, тени не менять») —
  выемка осталась родная, мягкая; альфа-форма (прозрачные углы) взята из прежнего
  экстракта, цвет — из edit-версии (механический композит в `process_ui.py`).
- В CLAUDE.md зафиксировано правило: игровую графику никогда не рисовать программно —
  только через утилиту генерации Gemini; скриптами — лишь механическая обработка
  сгенерированного (кроп/ресайз/альфа/9-slice).

## 2026-08-10 (ночь, вторая итерация) — вёрстка и вдавливание по замечаниям

- **Панель ввода**: единый задник с выемкой-лотком, извлечён из концепта одним куском
  (`extract_region --part` «panel bar with recessed tray, without tiles/button») —
  сборка из двух спрайтов (панель + отдельное углубление) убрана. Кнопка сброса —
  круглая в стиле «Принять» (генерация с рефом на пилюлю).
- **Верх/низ**: полосы снова ограниченной ширины со скруглениями (верх 780, низ 660 —
  по концепту), контент верхней отцентрован по вертикали (панель y=364, контент y=363).
- **Поле**: нижняя граница слайса подложки содержит запечённую тень — панель удлинена
  вниз (500×576, центр y=-30), визуальные отступы вокруг сетки выровнялись.
- **Тени плиток**: причина «обрезанных» теней — равная глубина отрисовки: нижняя
  плитка рисовалась поверх тени верхней. Теперь depth = 10 + row*0.1 (верхние ряды
  позже), плюс запас канвы под тень (margin 5%, слой BothStretch(-5)).
- **Вдавливание кнопок** переделано с подмены спрайта на анимацию: мультитрековый
  `AnimationClip` в стейте `pressed` — сдвиг `layout/offsetMin/Max` вниз на 4px +
  проявление тонированного тёмным того же спрайта (затемнение по силуэту, двоения
  нет, caption едет вместе с кнопкой). У плиток сдвига нет (их layout анимирует JS),
  только затемнение. `*_pressed.png`-варианты удалены из ассетов и конвейера.
- Все 7 сьютов зелёные; скрин зажатой кнопки — 09_pressed.png.

## 2026-08-10 (ночь) — правки вёрстки по замечаниям + разбор «мёртвого ввода»

- **Ввод**: по жалобе «ничего не нажимается» добавлен UI-тест
  `SceneLoadedFromAssetRespondsToClicks`, повторяющий путь реального приложения
  (загрузка `WordFall.scn` как в `GameApplication::OnStarted`, реальные клики через
  окно) — зелёный. В текущей сборке ввод работает; вероятная причина у контрибьютора —
  бинарь/ассеты из середины переделки. Если повторится на свежей сборке — нужен
  сценарий запуска (десктоп/веб, ресайз окна).
- **Панели сверху/снизу**: ширина 1400 (> экрана) — края уходят за кадр, как в
  концепте; обе одинаковые.
- **Панель слова**: ширина = подложке поля (500), внутри углубление-лоток под плашки
  (`ui_panel_inset` — сгенерирован Gemini: recessed tray с внутренней тенью) и секция
  кнопок ПРИНЯТЬ + крестик. Слоты ужимаются динамически, если слово длинное.
- **Поле**: рамка подложки тоньше (панель 500×556, отступ ~12), новая подложка
  с выраженной тенью (генерация с рефом на борд концепта).
- **Плитки**: пере-извлечены из концепта вместе с родной тенью
  (`extract_region --part --transparent`, «tile with its drop shadow») — белые
  пиксели-артефакты ушли вместе с ручным флуд-кеингом, тень запечена.
- Клики кнопок в UI-тестах перенесены на новые координаты (ПРИНЯТЬ (135,295),
  крестик (225,295)). Все 7 сьютов зелёные.

## 2026-08-10 (вечер) — приведение UI к концепту main_gameplay.png

Интерфейс сильно расходился с концептом — переделал вёрстку, арт и добавил анимации.

### Арт (Gemini Nano Banana 2, `o2/Tools/ImageGen`)
- Первая попытка была без утилиты (в чекауте не было API-ключа — программные PIL-спрайты
  контрибьютор забраковал). Ключ получен от контрибьютора → положен в
  `o2/Tools/ImageGen/api_key.txt`, весь арт переделан утилитой.
- Из концепта извлечены `extract_region --part --transparent`: 5 круглых кнопок бустеров
  (бейдж зарядов вычищался промптом; shuffle/hint дочищены `edit_image` + повторное
  восстановление альфы), пустая плитка (без буквы/номинала), ледяная плитка, пилюля
  ПРИНЯТЬ (без текста), коричневый бокс счётчика, заливка прогресс-бара (текст 260/300
  снят `edit_image`), бейдж зарядов.
- Сгенерированы `generate_transparent` с рефами на концепт: крестик сброса, рамка выбора
  плитки, кремовая и коричневая панели-подложки, стикеры пауэрапов (бомба/ракета/палочка;
  ракета и палочка с первого раза вышли «кнопками» из-за рефа бустера — перегенерены с
  рефом на стикер бомбы).
- Калибровка координат концепта: сетки поверх кропов + автодетект центров кругов бустеров
  по цветовому профилю колонок (795/1030/1265/1500/1733, y 1552).
- `Work/Art/process_ui.py` — механический конвейер: чистка альфы flood-fill от углов,
  кадрирование, сборка 9-slice (углы без искажений), pressed-варианты (затемнение+сдвиг),
  финальные размеры. Сырьё — `Work/Art/gen/`, превью — `Work/Art/ui/_sheet.png`.
- Легаси-спрайты старого стиля удалены из Assets.

### Шрифт
- `Assets/WordFall/game_font.ttf` — статический инстанс SF Compact Rounded wght=740
  (fontTools instancer), жирный округлый, кириллица полная — близко к концепту.
  ⚠ Системный шрифт Apple: для прототипа ок, в релизе заменить свободным (Baloo 2/Nunito).

### Вёрстка (bootstrap)
- Верхняя панель: единая кремовая подложка, слева «Уровень 1», прогресс-бар очков
  (трек + оранжевая заливка, ресайз из JS, min 34px) с «31/250», бокс «Ходы: N».
- Панель слова: широкая подложка; слово набирается мини-плитками-слотами (буква+номинал),
  не текстом; ПРИНЯТЬ — пилюля из концепта; сброс — кнопка-крестик.
- Поле: коричневая подложка-панель; плитки/лёд/рамка из концепта, тени запечены в спрайты;
  лёд теперь ПОД буквой — буква поверх кристалла синеет (как в концепте).
- Бустеры: подложка-полоса, круглые кнопки из концепта 78px с шагом 90, бейдж-кружок
  зарядов сверху-справа (число без «x»).
- Задачи: та же панель в новом стиле, без пересечений с соседями; ModeLabel перенесён
  под панель задач.
- 9-slice: размеры спрайтов подобраны под фактические размеры виджетов — границы слайсов
  нигде не сжимаются.

### Анимации
- Перелёт буквы в панель слова: клик по плитке → флаер (пул 4) летит по квадратичному
  безье (контрольная точка выше и сбоку) со smoothstep, размер 64→48 со «вспуханием»
  в полёте; слот открывается при приземлении. Баг «слот не открылся» (SyncWord до
  удаления полёта из списка) пойман UI-тестом и исправлен.
- Вдавливание кнопок: слой `pressed` + `AddState("pressed",
  EaseInOut(layer/pressed/transparency, 0.05s))` на всех кнопках (пилюли, крестик,
  бустеры, рестарт; плитки — полупрозрачное затемнение).

### Тесты
- Обновлены: скрин выбора ждёт приземления букв и проверяет включённость слотов;
  новый тест PressedButtonShowsPressIn (скрин зажатой ПРИНЯТЬ 09_pressed.png);
  SceneBuilds без изменений (имена виджетов сохранены). Клики-координаты не менялись.
- Итог: все 7 сьютов Game/GameUI зелёные. Бок-о-бок сравнение с концептом —
  `scratchpad/side_by_side` (стиль совпадает).

## 2026-08-10 — доработка по обновлённому ГДД

Обновлённый ГДД добавил: пауэрапы за длинные слова, систему заданий уровней, кампанию
из нескольких уровней. Сопоставил с кодом и доделал недостающее.

### Пауэрапы за длинные слова (бомба/ракета/палочка)
- Модель: `tile.powerup`, `PowerupForLength` (5→бомба, 6→ракета, 7+→палочка); после
  сжигания слова бонус прикрепляется к плитке, занявшей клетку последней буквы.
- Срабатывание при включении плитки-носителя в слово (`_ActivatePowerups`): бомба
  уничтожает 3×3 (очки взорванных букв — плоско в счёт, лёд сносится вместе с плитками);
  ракета крестом и палочка по одинаковым буквам «активируют» плитки — очки в счёт, лёд
  снят, плитки остаются (по ГДД). Взорванные клетки не получают повторный урон льду от
  8-соседства слова (skip-набор `destroyedKeys`).
- Вью: слои-бейджи bomb/rocket/wand на кнопках плиток (LeftTop 30×30), «+N» показывает
  полный `gain` (слово + пауэрапы).
- Спрайты: ключа Gemini в среде нет — нарисовал бейджи программно PIL-скриптом
  `Work/Art/make_powerups.py` в стиле бустеров (круглый бейдж, кремовая заливка,
  коричневый контур), проверил визуально.

### Система заданий и кампания
- Задания в конфиге уровня: `word` (точечное слово, матч с джокерами), `length`
  (N слов из L букв), `powerup` (заработать N бонусов вида), `clearIce` (разбить весь
  лёд; пересчёт и после молотка). Очки — обязательное финальное условие: `_CheckWin` =
  очки ≥ цели И все задания закрыты.
- Сид: `NewGame` выкладывает одно случайное слово из `word`-заданий горизонтально в
  средних рядах (`_SeedWord`); лёд клетки сида обходит.
- Кампания `WordFallConfig.levels` — 3 уровня (КОТ+лёд / ЧАШКА+бомба / РАКЕТА+ракета+лёд);
  `WordFallConfig.level` остался песочницей без заданий для старых тестов.
- UI: панель «ЗАДАЧИ» слева (Task0..4, выполненные зелёные с «+»), «Уровень N» в HUD,
  попап с «ДАЛЬШЕ»/«СНАЧАЛА»/«ЕЩЁ РАЗ» и переходом по уровням. Молоток может закрыть
  последний лёд — попап победы показывается и из режима бустера.
- Словарь: + КРЕМ, БАНКА, СТРАХ, КРЫШКА (примеры слов заданий из ГДД).
- Мелочь: «+N» перекрывался кнопкой ПРИНЯТЬ — сдвинут внутрь панели слова (по скриншоту).

### Тесты
- +12 headless (`WordFallModelTests`): заработка бомбы/ракеты/палочки и клетка прикрепления,
  взрыв 3×3 с очками, крест ракеты (буквы остаются, лёд снят), палочка по одинаковым
  буквам, сид слова из задания (и что лёд его не накрывает), прогресс `word`/`length`/
  `powerup`/`clearIce`, победа только при задачах+очках, валидность конфигов кампании.
- +2 UI (`WordFallUITests`): панель задач и номер уровня; бомба после 5-буквенного слова
  (модель + включённый слой бейджа). Тест победы переписан: задачи закрываются читом
  (`DebugCompleteTasks`), после «ДАЛЬШЕ» стартует уровень 2.
- Итог: GameTests 34 и GameUITests 8 (WordFall) — зелёные, полный прогон собранных суит зелёный
  (движковые o2*-суиты в этом дереве не собраны, изменения их не касаются).

## 2026-08-07

### Подготовка
- Очистил/создал `Work/` с подпапками `Art/`, `ScreenShots/`, `Concepts/`.
- Изучил структуру проекта: `Sources/Game` (точка входа GameApplication), `Sources/GameTests`
  (headless), `Sources/GameUITests` (реальное окно + AppTestDriver).
- Фоновым агентом составил карту JS-API движка. Ключевые ограничения, повлиявшие на
  архитектуру: из JS нет `o2Input`/`o2Scene`/`o2Assets`; единственное событие — `Button.onClick`;
  `actor.transform` не биндится (только `GetTransform()`); виджеты Toggle/EditBox и пр. не биндятся.
- Нашёл эталон: `git show b74fa9e` — Dragon Defense (bootstrap-компонент строит сцену в OnStart,
  JS-контроллер находит виджеты по путям).

### GDD и арт
- Написал `Work/GDD.md`: поле 7×8, свободный выбор букв, кластерный множитель по 8-соседству,
  множитель длины, мешок 100 плиток без Ъ/Ё, лёд, 5 бустеров, уровень 300 очков / 12 ходов.
- Сгенерировал концепты (`Work/Concepts/`): основной геймплей и окно победы (единый стиль через
  `ref_paths`). Первая генерация кнопки молотка вышла с запечённым бейджем «3» — перегенерил без
  бейджа (счётчики зарядов — динамические лейблы).
- Сгенерировал спрайты (`Work/Art/`): плитка, плитка-выделение, лёд, 5 бустеров, панель, кнопка,
  попап, фон, звезда. Подготовка в `Assets/WordFall/Sprites/` питон-скриптом: трим альфы + ресайз.
- Проверил `debugFont.ttf` — кириллица покрыта полностью (66/66), отдельный шрифт не нужен.
- Композинг теоретического экрана (`Work/composite_main.png`) сошёлся с концептом — координаты
  зафиксированы: ячейка 68, плитка 64, центр поля (0,−20), HUD y=360, слово y=295, бустеры y=−350.

### Код
- `Sources/Game/WordFall/WordFallBootstrap.{h,cpp}` — компонент-бутстрап: слои BG/UI, камера
  1280×800, фон, 56 кнопок-плиток (слои back/sel/letter/points/ice), HUD, панель слова, бустеры,
  скрытый попап; вешает `ScriptableComponent` → `Scripts/WordFallGame.js`.
- `GameApplication::OnStarted` — сохраняет bootstrap-сцену `Assets/WordFall.scn` (если нет) и
  запускает её; редактор открывает ту же сцену.
- `Assets/Scripts/WordFallGame.js` — `WordModel` (чистая логика: мешок с анти-клином по гласным,
  выбор с отсечением хвоста, словарь ~460 слов с джокер-матчингом, очки base×len×cluster,
  гравитация+спавн, 5 бустеров, win/lose) + `WordFallGame` (вью-контроллер: onClick, синк модели
  в виджеты, анимация падения через offsets лэйаута, режим прицела бустеров, попап, рестарт).

### Тесты
- `GameTests/WordFallModelTests.cpp` — 22 headless-теста модели (мешок, детерминизм по сиду,
  анти-клин, выбор, лёд, словарь/джокер, кластеры, очки, гравитация, win/lose, все бустеры).
- `GameUITests/WordFallUITests.cpp` — 6 тестов с реальными кликами: сборка сцены, выбор слова,
  принятие с очками, сброс, молоток, победный попап + рестарт. Скриншоты в `Work/ScreenShots/`.
- Итог: **все 326 тестов проекта зелёные** (в т.ч. 22+6 новых). Игра запускается вживую.

### Сложности и решения
1. **Strict mode классов**: `WordFall_instance = this` в методе класса кидает ReferenceError
   (тела классов всегда strict). Решение — `globalThis.WordFall_instance = this`.
2. **`GetParent()` возвращает WeakRef** — в JS это непрозрачный объект без методов актора.
   Решение — относительные пути `GetChild("../Board/...")` от актора Game (как в Dragon Defense).
3. **Sliced-кнопки разваливались**: slice border 50px при высоте кнопки 56px. Решение — уменьшил
   исходники (160×104) и slice до 22/24.
4. **CMake GLOB** не подхватывает новые .cpp без реконфига — `cmake --preset mac` перед сборкой.
5. **Первый lookup JS-ассета в headless-тесте возвращал null** — обошёл чтением исходника напрямую
   (`InFile` + `o2Scripts.Parse`), как делает FoxModelTests.
6. Ошибка арифметики в тесте мешка (ожидал 98, в мешке 100) — исправил тест, модель верна.

## 2026-08-14 — Новый арт по концепту, портретный режим

### Арт
- Источник — `ArtSrc/concept.png` (768×1376) и авторская нарезка `ArtSrc/composer-layers/`
  (слои отресайзены нарезкой в избыточные размеры: плитка 872×846, лоток 1964×442 и т.п.).
- `Work/Art/process_concept.py` — механический конвейер: чистка альфы от шума нарезки
  (порог 24, для градиентной заливки бара 96), кроп по bbox, приведение к рендер-размерам
  портрета 1:1 с концептом; подложка поля собрана в 9-slice 128×128 (слайс 48 — углы в
  нативном размере). Итоговые спрайты: плитка 94 (виджет 84 + поля -5), лоток 505×94,
  кнопки ✓/✗ 94, боксы уровня/ходов 136/142×106, панель очков 385×112 (выемка бара
  343×44 со смещением -6), заливка 335×36 (без слайса — градиент сжимается целиком),
  панель задач 712×176 (тёмная шапка 42), бустеры 92.
- Маппинг бустеров: молоток←bomb_btn, перемешать←shuffle_btn, подсказка←idea_btn,
  джокер←swap_btn; кнопка x2 и выделенная плитка (золотая) догенерированы Gemini по
  референсам слоёв; у выделенной плитки альфа взята из оригинального слоя. Бейдж зарядов
  перегенерирован в тёмно-синем стиле (старый сливался с яркими кнопками).
- Шрифты: M PLUS Rounded 1c (OFL, полная кириллица) — Black (заголовки/цифры/буквы) и
  ExtraBold (текст), сабсет до латиницы+кириллицы (~58KB), выбор через параметр `heavy`
  фабрики. Удалены: ui_wordbar, ui_bar_track, ui_panel_cream, popup_panel, ui_btn_cross.

### Портрет
- Логический экран 768×1376 == концепт; камера fitted, окно 768×1376 (macOS ужимает по
  высоте — картинка масштабируется, ввод конвертируется слоем листенеров через камеру).
- Раскладка: Hud (0.5,1) −85 (боксы уровня/очков/ходов с подписями), Tasks (0.5,1) −257
  (712×178, задачи в 2 колонки), WordBar (0.5,1) −419 (лоток слева, ✓/✗ справа),
  Board центр (0,−170) — клетка 88, плитка 84, 7×8; Boosters (0.5,0) +90, шаг 130.
- UI-тесты кликают в оконных координатах: `ToWindow(world) = world × min(окно/камера)` —
  иначе клики мимо при ужатом окне. iOS plist переведён на портрет; Android уже был
  sensorPortrait; wasm — фуллскрин-канвас.

### Сложности и решения
1. **Окно выше экрана**: macOS ужал окно 768×1376 до 768×1257 — все клики тестов мимо.
   Ввод в o2 конвертируется через камеру слоя листенеров → тестам нужен масштаб
   min(res.x/768, res.y/1376).
2. **Размер частицы = множитель спрайта** (imageSize × particle.size), а не пиксели:
   старые значения 14/22 давали кляксы 900–1400px — на кремовом фоне были незаметны,
   на синем залили экран. Теперь 0.35/0.55.
3. **Удаление BuiltAssets/** снесло и EditorData — o2EditorUITests падали сегфолтом в
   LoadStyle. Лечится `cmake --build --preset mac --target BuildEditorAssets`.
4. **BuiltAssets для wasm/android/ios** собираются host-утилитой:
   `Bin/Mac/AssetsBuilder -platform <P> -source Assets/ -target BuiltAssets/<P>/Data/
   -target-tree BuiltAssets/<P>/Data.json -compressor-config o2/CompressToolsConfig.json`.
5. Удалённый `Assets/WordFall.scn` регенерируется бридж-тестом, но UI-тесты видят его
   только после пересборки ассетов (порядок: тесты → пересборка → тесты).

### Итог
Все 327 тестов зелёные; сборки mac/wasm/android/ios-sim проходят. Скриншоты в
`Work/ScreenShots/` соответствуют концепту (поле темнее концепта — таким дан слой
field_bg в авторской нарезке).

## 2026-08-15 — Вторая итерация арта, зазоры между плитками

- Автор обновил нарезку `ArtSrc/composer-layers` (теперь в масштабе composed-канвы 1024,
  контент 572×1024; коэффициент к экрану 768×1376 — ×1.343). Новый стиль: чёткий обвод
  без внешней тени у плитки, короткая панель задач с тонкой шапкой, поле выше.
- `Work/Art/process_concept.py` переписан под новую нарезку; панель поля — 9-slice
  112×112 (слайс 40), панель задач тянется по вертикали 9-slice'ом (шапка в верхнем
  борте, BorderI(20,16,20,36)), заливка бара 325×34 под выемку новой панели очков.
- Кнопка x2 и выделенная плитка перегенерированы Gemini по новым рефам (старые были
  в стиле прошлой нарезки); альфа выделенной плитки — из нового letter_bg.
- Отступы: шаг сетки 96 при плитке 82 — зазор между плитками 14, столько же от плитки
  до борта панели (панель = габарит ячеек + 7, спрайт ложится ещё +7). В лотке ввода
  слоты 54 с зазором 10. kTileLayout теперь без полей (обрезка по обводу).
- Раскладка сдвинута: задачи (713×140, −238), лоток (490×106, −355), поле центр −121
  (панель 686×782), бустеры 120×114 с шагом 132. Клики в тестах обновлены.
- Все 328 тестов зелёные; пребилд-ассеты wasm/android/ios пересобраны.

## 2026-08-15 — Экран геймплея как прототип, адаптивные якоря

- Экран геймплея теперь единый прототип `Assets/WordFall/Prototypes/GameScreen.proto`
  (корень WordFall: BG + Screen со всеми секциями, плитками, слотами, бустерами,
  Fx-пулами, попапом и вьюхами + Vfx). Bootstrap на старте инстанцирует его на сцену;
  при отсутствии ассета (свежий чекаут) — собирает кодом и сохраняет (desktop).
  Зависимости вьюх (serviceActor/vfxActor) инжектятся после инстанцирования; сами
  вьюхи и их публичные поля запечены в прототип и редактируются в редакторе.
- Параметры раскладки вынесены в поля вьюх: сетка букв (cellSize/tileSize/boardCenterY
  в WordFallBoardView — вьюха раскладывает плитки на старте), строки задач
  (firstRowY/rowStep в WordFallTasksView), лоток/слоты (WordFallWordPanelView),
  бар (WordFallHudView).
- Якоря переведены на адаптивные: полосы Hud/Tasks/WordBar/Boosters растянуты по
  ширине (offsets от краёв), боксы уровня/ходов и кнопки принять/сброс привязаны
  к своим краям, лоток тянется до кнопок, Fx/Popup — full-stretch; поле — сетка
  фиксированного размера по центру. У кнопок со стейтом вдавливания якоря задаются
  до AddPressedState — треки стейта пишут конечные офсеты лэйаута.
- **Починен старый баг движка** («повторный клон Widget-актора из ActorAsset даёт
  пустые инстансы»): при клонировании актор копировал ссылку на объект слоя сцены,
  а слой умирает вместе со сценой — инстансы после пересоздания сцены не рисовались
  и не получали ввод. Теперь клон перепривязывает слой по имени к текущей сцене
  (симметрично десериализации). Actor.cpp (копирующий конструктор + operator=),
  доки o2 обновлены (ru+en). Регрессия: `GameUITests/WordFallProto` (4 теста:
  контроль прямой сборки, повторное инстанцирование, после Save, после
  десериализации — все с проверкой кликабельности).
- Все 330 тестов зелёные; пребилд-ассеты wasm/android/ios пересобраны.

## 2026-08-15 (2) — Правки вёрстки: иерархия, размеры, HorizontalProgress

- Иерархия сцены: зависимые объекты сложены внутрь родителей — LevelBox/MovesBox
  (Caption+Value внутри), ScorePanel (Caption+Bar+ScoreLabel), Tray (Slot0..7 +
  GainLabel). Слоты теперь в локальных координатах лотка (trayCenterX=0, полёт
  флаеров — через поле trayWorldX).
- Прогресс очков — виджет движка `HorizontalProgress` (слой "bar" = заливка);
  в движке SetValue/SetValueForcible/GetValue помечены @SCRIPTABLE — раньше JS
  их не видел. Вьюха ставит долю (min 0.05, чтобы градиент не схлопывался).
- Плитки крупнее: 88 при шаге 96 (зазор 8, до борта панели 11); спрайты плитки
  и золотого выделения пересобраны в 88, буква 44. Кнопки ✓/✗ уменьшены до
  88×95 / 92×92 и прижаты к правому краю.
- Вертикальный стек исправлен: задачи 168..296, лоток 304..410 — поле ввода
  больше не налезает на панель задач.
- Все 330 тестов зелёные; GameScreen.proto и сцена регенерированы, пребилд-ассеты
  мобильных платформ пересобраны.

## 2026-08-15 (3) — Крупнее буквы в поле ввода, широкий фон

- Слоты набираемого слова: 54 → 64, буква 28 → 36, номинал 12 (kSlotSize фабрики
  и slotSize вьюхи).
- Фон перегенерирован Gemini по рефу старого (та же мягкая низкополигональная
  голубизна) в 2 раза шире — 1536×1376; BG-актор растянут до 2×ширины экрана,
  чтобы закрывать поля на широких соотношениях сторон.
- Все 330 тестов зелёные, прототип/сцена регенерированы, мобильные пребилд-ассеты
  пересобраны.

## 2026-08-15 (4) — 9-slice бара, новая хореография очков, фикс прозрачности слоёв

- Заливка прогресс-бара — 9-slice (слайс 17 по торцам): скруглённые каёмки не
  тянутся, середина градиента растягивается; минимум заливки 0.12.
- Хореография очков упрощена: полёт цифр с каждой плашки убран. Теперь «+N»
  показывается у лотка при сгорании букв, затем единственный полёт итога в
  прогресс-бар: белые цифры с бело-голубым свечением (ui_fx_flash, материал
  BlendMode::Add), на прилёте — бело-голубые искры (эмиттер ScoreHit с
  ParticlesColorEffect: белый → голубой → прозрачный) и вспышка.
- В движке @SCRIPTABLE у HorizontalProgress::SetValue/SetValueForcible/GetValue.
- **Починен баг движка**: `WidgetLayer::UpdateResTransparency` затирал авторскую
  альфу цвета drawable (`SetTransparency` пишет прямо в mColor.a) — у инстансов
  прототипов полупрозрачные спрайты (затемнение попапа) становились
  непрозрачными. Теперь прозрачность слоя применяется через альфу override-цвета
  drawable: авторская альфа сохраняется и перемножается. Регрессия —
  `WordFallProto.TranslucentSpriteColorSurvivesInstantiation`; редакторский тест
  дерева читает теперь res-прозрачность слоя. Доки ui.md обновлены (ru+en).
- Диагностика заняла время из-за ловушки: BuiltAssets никогда не удаляет —
  в кэше жил устаревший GameScreen.proto, и «эксперименты» с игровым кодом
  не влияли на инстанцируемую сцену.
- Все 330 тестов зелёные, скриншоты сверены, мобильные пребилд-ассеты пересобраны.

## 2026-08-15 (5) — Откат смены семантики прозрачности: она ломала редактор

- Вчерашний фикс «прозрачность слоя через override-цвет» сломал вид редактора:
  его стили опирались на старое поведение (авторская альфа спрайтов затирается
  первым апдейтом прозрачности — панели рисуются непрозрачными). Откатил
  UpdateResTransparency к записи в альфу цвета drawable; редакторский тест
  дерева вернулся к чтению drawable.
- Правильный фикс исходного бага (затемнение попапа непрозрачно у инстансов
  прототипа) — конвенционный: полупрозрачность задаётся ПРОЗРАЧНОСТЬЮ ВИДЖЕТА
  (сериализуется), а не альфой цвета спрайта. В движке добавлен пересчёт
  UpdateTransparency() в конце копирующего конструктора и operator= виджета —
  при клоне слои переустанавливались с прозрачностью 1, и скопированное
  значение прозрачности не доезжало до drawable'ов.
- Dim попапа: цвет непрозрачный + dim->SetTransparency(170/255). Регрессия
  переписана: WordFallProto.WidgetTransparencySurvivesInstantiation.
- Доки ui.md (ru+en): полупрозрачность — прозрачностью виджета/слоя, не альфой
  цвета спрайта. Все 330 тестов зелёные, редактор пересобран.

## 2026-08-15 (6) — Библиотека прототипов вместо монолитного экрана

- Экран разделён на прототипы: Tile (плашка буквы), WordSlot (плашка лотка),
  Booster, IconButton (принять/сброс, спрайт меняется на инстансе), PillButton
  (кнопка попапа), TaskRow (строка задания, Widget+Text), FxFlash/FxStar/FxGlow/
  FxBeam (эффекты). GameScreen.proto теперь содержит их инстансы с прототип-
  линками (диффы — файл ужался с 2МБ до 1МБ); правка прототипа в редакторе
  обновляет все инстансы.
- `InstantiatePart(path, builder)` — общий путь: инстанс из ассета, при отсутствии
  ассета сборка билдером и сохранение (desktop; сохранённый ассет сразу в кэше).
- Кнопки перестроены: внешний виджет позиционируется якорями, внутренняя кнопка
  со стейтом вдавливания — в стабильном full-stretch лэйауте. Грабли: у внутренней
  кнопки глубина отрисовки должна быть ЯВНОЙ — наследуемая глубина в повторно
  включаемом поддереве (попап) сортируется непредсказуемо, кнопка «ДАЛЬШЕ»
  рисовалась под панелью. Диагностика через серию изолированных тестов
  (пилюля кликабельна: напрямую, из ассета, вложенно, после Save, в выключенном
  контейнере) — все проходили, ломался только полный сценарий: указало на
  порядок отрисовки, а не на прототипирование.
- Вьюхи: клики через AcceptBtn/Btn, ClearBtn/Btn, RestartBtn/Btn; строки задач
  Task№/Text. Все 331 тест зелёные, скриншоты сверены, мобильные ассеты
  пересобраны.

## 2026-08-15 (7) — Новая механика выбора: буквы улетают в лоток

- Выбранные буквы больше не подсвечиваются на поле — плашка скрывается
  (ячейка пустеет), флаер летит по безье в слот лотка; клик по слоту лотка
  возвращает букву (и хвост выбора) на поле. Принятое слово: буквы лотка
  друг за другом летят в прогресс-бар, вспышка на прилёте, искры и «+N».
- Скрытие выбранных плашек через `WidgetLayer` enabled вскрыло баг движка:
  `WidgetLayer::mEnabled` не сериализовался и не копировался при клоне —
  вся доска приходила «золотой» (слой sel включён). Починено в o2
  (@SERIALIZABLE + копирование в клон-путях).
- `Widget::SetTransparency` помечен @SCRIPTABLE — вьюхи гасят «+N» из JS
  (не-SCRIPTABLE вызов молча стопорил секвенсор — попап не показывался).

## 2026-08-15 (8) — Компонент FlightTrajectory и прототип летящей буквы

- Новый компонент `FlightTrajectory` (Sources/Game/WordFall): сплайн с шириной
  ключей (коридор случайного смещения), публичный параметр `position` 0..1
  (@ANIMATABLE), стартовая и финишная точки. Фактические концы сплайна
  геометрически отображаются в целевые точки (кэш Basis: поворот+масштаб+
  перенос), позиция пишется в трансформацию актора на апдейте (виджетам — в
  офсеты лэйаута). Случайное смещение перевыбирается по `ResetRandomOffset()`
  или при возврате `position` в 0. 9 headless-тестов GameTests/FlightTrajectory.
- `FlightTrajectoryViewer` в Sources/Editor (EditorLib): сплайн редактируется
  `SplineTool` прямо в сцене (origin — стартовая точка), по образцу
  MeshComponentViewer/ParticlesSplineEffectViewer.
- Прототип `FxFlyingLetter.proto`: плашка (back/letter/points) + AnimationComponent
  + FlightTrajectory + дочерний актор Sparks с ParticlesEmitterComponent.
  Анимация «flight» (ассет `FxFlyingLetterFlight.anim`): трек
  `component/FlightTrajectory/position` 0→1, скейл (вспухание→сжатие к бару),
  крен `transform/angleDegrees`, саб-трек искр
  `child/Sparks/component/o2::ParticlesEmitterComponent`. FxView вместо ручного
  лерпа зовёт `SetPoints`+`SetPosition(0)`+`RewindAndPlay("flight")`.
- Грабли: (1) стейт сериализует клип ссылкой на AnimationAsset — встроенный
  в память клип терялся при загрузке прототипа с диска, клип сохраняется
  .anim-ассетом; (2) путь саб-трека требует полное имя типа
  (`o2::ParticlesEmitterComponent`); (3) `transform/scale` — Vec3F, Vec2F-трек
  вешается на `transform/scale2D`; (4) ParticlesEmitterComponent прямо на
  виджете падал при клоне (OnTransformUpdated на недостроенном WidgetLayout) —
  эмиттер вынесен на дочерний актор.
- Фиксы движка (+2 регрессионных теста): AnimationComponent::RegTrack — warning
  вместо null-deref при несовпадении типа трека и поля; AnimationSubTrack::
  Player::SetTarget — guard на неразрешённый путь. Все 332 теста зелёные,
  скриншот 03b_letter_flight.png (полёт со стаггером и креном), мобильные
  ассеты пересобраны.

## 2026-08-15 (9) — FlightTrajectory переехал в движок, эффект полёта доработан

- Компонент перенесён в o2: `o2::FlightTrajectoryComponent`
  (Framework/Sources/o2/Scene/Components), вьювер — в o2Editor
  (Properties/Objects/Components/FlightTrajectoryViewer, SplineTool), тесты —
  в o2SystemTests (9 штук). Категория «Animation», имя «Flight trajectory»,
  иконка `ui/UI4_trajectory_component.png` — сгенерирована Gemini в стиле
  существующих иконок (20×20, белый line-art: пунктирная дуга от точки к
  стрелке), сверена визуально с рядом соседних иконок в реальном масштабе.
  Доки components.md (ru+en) дополнены. Игра/JS/треки переведены на полное имя
  `o2::FlightTrajectoryComponent`.
- Эффект полёта букв в бар доработан:
  - бесшовный старт — флаеры ставятся на места слотов лотка синхронно с их
    гашением (SetPoints + SetPosition(0) + Enable в кадре принятия), буква
    стартует в зелёной подсветке собранного слова и за первую половину полёта
    перекрашивается в обычный тёмный цвет (JS-лерп);
  - превращение в звезду: слой star (крупнее плашки) в клипе проявляется на
    45–70% пути, слои плашки (back/letter/points) растворяются на 50–80%;
    звезда влетает в кончик текущей заливки прогресс-бара
    (HudView.BarTipWorldX) и гаснет за 0.08с после влёта;
  - на влёте выпускается пучок искр в стороны: второй дочерний эмиттер Burst
    (300 частиц/с × 0.1с, скорость 260) запускается саб-треком со сдвигом
    beginTime = конец полёта; флаер прячется через 0.5с после прилёта.
- Грабли: битое ассет-дерево (rm файлов без пересборки дерева) отдаёт
  «призрачные» ассеты из кэша — Save пропускается и .anim не попадает на диск;
  после ручного удаления прототипов обязательно прогонять AssetsBuilder.
  После полёта слои остаются растворёнными — _PlaceAtStart возвращает их
  прозрачности перед новым стартом.
- Все 332 теста зелёные, скриншоты 03b (зелёная буква на месте слота +
  перекраска в полёте) и 03c (звезда у кончика бара с пучком искр) сверены,
  мобильные ассеты пересобраны.

## 2026-08-15 (10) — Редактор: меню Run и скорость игры

- Меню Run пополнено: Play - stop (P), Frame step (Cmd+P), Speed up (Cmd+«+»),
  Slow down (Cmd+«−») — дублируют панель запуска.
- В панель PlayStop добавлено управление скоростью игры: [−] [1.0] [+],
  шкала из 10 множителей 0.1 / 0.25 / 0.5 / 0.75 / 1.0 / 1.5 / 2 / 3 / 5 / 10.
  В движковой части — EditorApplication::SetGameTimeScale: множитель применяется
  к dt апдейта сцены и физики в режиме игры; сам редактор обновляется с
  нормальной скоростью.
- Попутный фикс движка: голые буквенные хоткеи (P, Delete) больше не
  срабатывают, пока сфокусировано текстовое поле —
  ShortcutKeysListenersManager::IsAllowedDuringTextInput пропускает при вводе
  только комбинации с Ctrl/Cmd/Alt и F-клавиши (юнит-тест в o2SystemTests;
  F-коды на Mac не непрерывны — список явный).
- Доки editor.md (ru+en) обновлены. Все 333 теста зелёные, редактор собран.

## 2026-08-15 (11) — Иконки кнопок скорости игры

- Кнопки «+»/«−» панели запуска переведены с текстовых слоёв на иконки в стиле
  панели: сгенерированы Gemini, обработаны до 20×20 (глиф 12px) и тонированы в
  три состояния по образцу step-кнопки — regular (235,255,253),
  hover (белый), pressed (бирюзовый 112,204,192);
  файлы `UI4_plus_btn_*` / `UI4_minus_btn_*`, стили "plus"/"minus" в
  EditorUIStyle (пересобираются автоматически по дате правки стилей).
- Проверено по скриншоту запущенного редактора: вес штриха и цвет совпадают с
  play/step, множитель «1.0» читается между кнопками, панель не разъезжается.
  Редакторные сьюты зелёные (109).

## 2026-08-15 (12) — Фиксы эффектов сбора слова

- Мигание букв после 2-3 сборов — клубок из трёх причин, все воспроизведены
  регрессионным тестом WordFallUI.ConsecutiveWordsKeepFlyingLettersVisible
  (3 сбора подряд + серия кадров 10_multi_word_a..d):
  1) хвост прошлой анимации полёта продолжал каждый кадр гасить слои: микшер
     AnimationComponent пишет значения стейта даже остановленным — теперь между
     полётами стейту выставляется вес 0 (микшер возвращает исходные значения),
     на старте — вес 1 + RewindAndPlay (в движке GetState/SetWeight/GetWeight
     помечены @SCRIPTABLE);
  2) у инстансов-клонов виджетов слои-цели анимаций биндились до появления
     слоёв (Actor копирует компоненты раньше слоёв Widget) — движковый фикс:
     ре-биндинг стейтов в конце Widget copy ctor и Widget::OnDeserialized;
     провальный биндинг больше не оставляет в микшерах null-цель (краш);
  3) UID новых ассетов генерились из глобального rand(), который партикл-
     эмиттеры ресидят фикс-сидом — прототипы из разных прогонов получали
     ОДИНАКОВЫЕ id (FxFlash == FxFlyingLetter), ссылки резолвились в чужой
     ассет; движковый фикс: UID::Randomize на собственном mt19937 (юнит-тест);
     плюс валидация «призрака» из дерева ассетов при сохранении .anim.
- Частицы у прогресс-бара рисовались под заливкой и фоном: акторы эмиттеров —
  самостоятельные drawable слоя с нулевой глубиной; теперь Sparks 62, Burst
  62.5, эмиттеры WordFallVfx 70 (поверх HUD, под попапом).
- «Стреляли вправо»: направление разлёта — это moveDirection (0° = вправо,
  разброс по умолчанию 45°), а SetInitialAngle крутит только спрайт; всем
  эмиттерам задан SetEmitParticlesMoveDirectionRange(360).
- Желтизна: спрайты vfx_spark/ui_fx_star/ui_fx_flash перегенерированы Gemini
  бело-голубыми (свечения на чёрном → альфа из яркости), луч ракеты
  перекрашен в голубой, всем искровым эмиттерам добавлен бело-голубой градиент
  затухания.
- Визуальная проверка по кадрам: буквы видимы на всех трёх сборах, звезда
  влетает в кончик заливки ПОВЕРХ бара, пучок искр круговой, вся палитра
  бело-голубая. Все 333 теста зелёные (свежая сборка и загрузка с диска),
  мобильные ассеты пересобраны.

## 2026-08-15 (13) — Лёд, камень и бонусы-плитки из новой графики

- Графика из ArtSrc/composer-layers: ледяная и каменная плашки
  (ui_ice/ui_stone — полноразмерные, буква рисуется поверх своим стилем:
  на льду синеет, на камне светлеет), бомба/ракета/фейерверк
  (powerup_bomb/rocket/fireworks), пилюля заливки прогресса. Палочка (wand)
  удалена из игры вместе со спрайтом.
- Механика (ядро, всё покрыто headless-тестами):
  - лёд разбивается выбором соседней буквы (сразу, 8-соседство), отмена
    выбора лёд не возвращает; сервис отдаёт GetLastIceBroken — вью искрит
    на осколках;
  - камень (stone) не выбирается и разбивается только бонусами; бомба/ракета
    снимают камень, плитка с буквой остаётся;
  - бонусы занимают слот вместо буквы (letter пустая, выбор Blocked), падают
    с гравитацией; активируются буквой принятого слова по соседству,
    бонус-плитка сгорает при активации;
  - бомба — 3×3 как раньше; ракета — удаляет одну случайную букву (цели в
    powerupsUsed[].targets); фейерверк — залп из 10 ракет по разным плиткам;
  - награды за длину: 5 — бомба, 6 — ракета, 7 — фейерверк; в генераторе
    уровней камни с 5-го уровня (до 4), пул задач wand→fireworks.
- Эффекты: прототип FxRocket (спрайт ракеты + FlightTrajectoryComponent +
  анимация полёта с креном и искровым следом + три цветных салюта
  суб-треками на прилёте), пул FxRocket0..9 для залпа фейерверка;
  WordFallVfx.PlayFirework — тройки розовый/жёлтый/голубой burst-эмиттеров,
  бомба и добивание тоже салютом. Пул FxStar и wand-эффекты удалены.
- Прогресс-бар: заливка — новая градиентная пилюля, отресайжена под точную
  высоту бара 34px (9-slice больше не плющит торцы).
- UI-тест RocketBonusFliesToTarget: камень не кликается, ракета летит по
  компоненту траектории, салют выпущен (скрины 11/11b). Все 333 теста
  зелёные (сборка и загрузка с диска), мобильные ассеты пересобраны.

## 2026-08-15 (14) — Пилюля прогресса и бонусы без подложки

- Заливка прогресс-бара была кривой: в progress_fill.png реальная пилюля
  118×26 сидела в холсте 191×56 с шумовой альфой по краям — getbbox не
  отсекал поля, и растяжение шло с огромными прозрачными полями. Пилюля
  вырезана по порогу альфы (>10), отресайжена с сохранением пропорций до
  высоты бара (154×34), 9-slice по фактическим торцам BorderI(14,0,14,0).
  Проверено крупным планом скриншота: торцы круглые, вертикаль не плющится.
- Бонусы бомба/ракета/фейерверк рисуются без плашки-подложки: слой back
  выключается на бонус-плитке (SyncTile) — на поле остаётся только иконка.
- Все 333 теста зелёные, мобильные ассеты пересобраны.

## 2026-08-15 (15) — Телепорт частиц, гравитация/торможение, деплой

- Баг «эффекты в случайный момент, буквы будто исчезают»: у эмиттеров частицы
  по умолчанию относительные (particlesRelative) — пул PlayAt переносил актор
  эмиттера, и ЖИВЫЕ искры прошлой вспышки телепортировались в новую точку,
  на миг накрывая плитку облаком (казалось, буква пропала). Все игровые
  эмиттеры переведены в мировые координаты (SetParticlesRelativity(false)) —
  бонус: след летящей плашки и шлейф ракеты теперь тянутся позади, а не летят
  вместе с актором. UI-тест SelectingNearIceKeepsNeighborsVisible: выбор рядом
  со льдом не трогает соседние плитки (скрин 12).
- В движок добавлен ParticlesDampingEffect (воздушное торможение,
  velocity *= 1−damping·dt; юнит-тесты Damping/Gravity в o2SystemTests).
  Всем искровым эффектам добавлены гравитация (−350…−600) и торможение
  (1.8…2.4): салюты разлетаются, тормозят и опадают. Проверено покадрово
  (11b/11c/11d): облако расширяется, оседает вниз, хвосты тянутся к земле.
- Все 334 теста зелёные, мобильные ассеты пересобраны.

## 2026-08-15 (16) — Полировка механики и хореографии, деплой v8

- Лёд и камень больше не реагируют на выбор: лёд скалывается только при
  ПРИНЯТИИ слова соседней сгоревшей буквой (DamageIceAround вернулся в
  AcceptWord), камень — только бонусами. GetLastIceBroken удалён; тесты ядра
  и UI переписаны (лёд/камень переживают выбор, скрин 12).
- Слоты лотка при вставке новой буквы съезжают на новые места плавно
  (targetX + лерп в Update, новые появляются сразу на месте).
- VFX: у всех градиентов альфа гаснет с трети жизни; частиц меньше в
  1.5–2 раза (burn 8, взрыв 24, салюты 12–14, следы 50–70/с); эффект
  «сгорания» больше не играет на клетках букв слова — только на снятой
  броне и сколотом льде.
- Подсказка выбирает буквы по одной (очередь в BoardView, шаг 0.14с) —
  буквы штатно улетают в лоток.
- Осыпание каскадом: плитки одной колонки стартуют снизу вверх с паузой
  fallCascade 0.05с.
- Бонусы этапами с общей паузой bonusPause 0.3: бомба — пауза, взрыв
  (зона гаснет сразу), пауза, обвал; ракета — пауза, взлёт того же спрайта
  (плитка бонуса гаснет без разрыва), медленный полёт 0.8с, попадание
  (клетка гаснет), пауза, обвал; фейерверк — залп с тем же циклом. Обвал
  всегда после последнего эффекта + пауза.
- Все 334 теста зелёные, мобильные ассеты пересобраны, деплой web+android.

## 2026-08-15 (17) — Логическая модель падения плиток (WordBoardMotion)

- Падение и спавн плиток вынесены из JS в C++ модель Core/WordBoardMotion:
  сетка офсетов (в клетках), скорость/каскад, колонка-стек — плитка никогда
  не опускается ниже плитки под собой (кламп на апдейте и на старте), поэтому
  наложения исключены при любых задержках. Спавны выстраиваются из-за верхней
  границы стопкой и скрыты (hidden), пока не вошли в поле — «фишки над полем»
  больше не видны. Повторный обвал во время анимации продолжает движение от
  текущих офсетов без телепортов; Configure не сбрасывает движение при тех же
  размерах поля.
- Сервис: OnUpdate двигает модель; SCRIPTABLE-фасад StartCollapseAnimation /
  IsCollapseAnimating / GetTileFallOffset / IsTileFallHidden /
  FinishCollapseAnimation; темп (fallSpeedCells, fallCascadeDelay) —
  @EDITOR_PROPERTY сервиса. BoardView больше не считает падение — каждый кадр
  отображает офсеты и видимость из модели.
- Список кейсов покрыт headless-тестами WordBoardMotionCase (8 шт): слово в
  ряду, каскад стартов, скрытые спавны, вертикальное слово, бомба 3×3 по трём
  колонкам, рестарт во время анимации, мгновенный Finish; инвариант «зазор
  между соседями колонки ≥ клетки» проверяется на каждом кадре симуляции.
- Контрольные кадры обвала после бомбы (13_fall_a/b/c): спавны не видны над
  полем, наложений нет, каскад читается, финал собран. Все 335 тестов зелёные,
  мобильные ассеты пересобраны.

## 2026-08-15 (18) — Промигивание плиток на старте обвала

- Новые буквы на один кадр показывались на конечных местах: в
  ApplyPendingCollapse SyncBoard обновлял доску, а офсеты модели падения
  применялись только в следующем Update. Порядок исправлен: сначала синк
  доски, затем в том же кадре старт отображения обвала с немедленным
  применением офсетов и скрытий (_ApplyCollapseView вызывается синхронно из
  _BeginCollapseView и переиспользуется в Update). Кадры 13_fall_a/b/c чистые,
  все 335 тестов зелёные, мобильные ассеты пересобраны.

## 2026-08-15 (19) — Колесо мыши на macOS

- На Mac RendererView отдавал scrollingDeltaY как есть: у тачпада/Magic Mouse
  это точные пиксели (частые, крупные), у классического колеса — строки
  (±1..3), в 40–100 раз меньше щелчка на Windows (120). Вместо эвристики по
  величине дельт используется системный флаг hasPreciseScrollingDeltas:
  точные дельты проходят как есть, строчные умножаются на
  Input::kWheelLineDelta (60, полщелчка Windows) — один щелчок мыши стал сопоставим с
  с жестом тачпада, тачпад не затронут. Логика вынесена в чистую
  Input::NormalizeWheelDelta (юнит-тест в o2SystemTests), доки input.md ru+en.

## 2026-08-26 (20) — Клик по вкладкам докинга после смены заголовка

- DockableWindow: кликабельная область вкладки (`mHeadDragAreaRect`, по ней
  работает CursorEventsArea заголовка) пересчитывалась только в
  `UpdateSelfTransform`, т.е. при инвалидации лэйаута окна. Смена caption
  («Animation - <ассет>») меняла ширину слоя вкладки и через
  `ArrangeChildWindows` сдвигала слои соседних вкладок, но лэйаут окна не
  трогала — вкладки рисовались по-новому, а клики ловились по старым ректам.
  Теперь рект пересчитывается из слоя вкладки в `SetTabWidth`,
  `SetTabState`, `SetNonTabState` (общий `UpdateHeadDragArea`), добавлен
  геттер `GetHeadDragAreaRect`. Регрессионный тест
  o2EditorTests/DockableWindowTabHitArea (без фикса падает), 346 тестов зелёные.

## 2026-08-26 (21) — Редактирование анимации FxFlyingLetter в редакторе

- Прототип сохранялся выключенным (`SetEnabled(false)` в билдере — инстансы и так
  выключает Bootstrap) и с совпадающими точками траектории. Билдер теперь отдаёт
  включённый прототип с точками (-160,-80)→(160,80); прототип и .anim перегенерированы.
- `FlightTrajectoryComponent::position` — теперь PROPERTY (сеттер `SetPosition`,
  поле `mPosition` @SERIALIZABLE): трек анимации пишет через прокси свойства,
  и актор двигается сразу при скраббинге в редакторе, где `OnUpdate` не
  вызывается. Тесты o2SystemTests/FlightTrajectory (скраб плеером без апдейта
  сцены, сериализация), доки components.md ru+en.
- Частицы саб-трека при скраббинге: `ParticlesEmitter::Evaluate` восстанавливал
  запечённый кадр в `mParticles`, но меш контейнера обновлялся только в `Update`,
  который в edit-режиме вызывает лишь вьюер выделенного эмиттера. Теперь после
  восстановления кадра контейнер синхронизируется на месте. Тесты
  o2SystemTests/ParticlesEmitterScrub и o2EditorUITests/AnimationWindowScrubUI
  (курсор таймлайна двигает актор и частицы), 348 тестов зелёные.
- Попутно замечено (не правил): `AnimationTrack<T>::Linear` даёт не линейную
  кривую (в середине 0.889 вместо 0.5) — тесты сравнивают с `GetValue`.
- Слои виджета «не рисуются» при скраббинге: клип FxFlyingLetter длиннее треков
  слоёв (саб-трек Burst начинается в конце полёта), а `Curve::Evaluate` и
  `AnimationTrack<T>::GetValue` (generic, Vec3F, Color4) за последним ключом
  экстраполировали последний сегмент — прозрачность уходила в −2.6, слои гасли,
  position улетал за 1. Теперь позиция клампится к диапазону ключей: трек держит
  крайние значения (Vec2F идёт через timeCurve). Доки animations.md ru+en.
  Тесты: o2UtilTests/Curve, o2SystemTests/AnimationTrackRange (float/Vec2F/Vec3F/
  Color4), o2EditorUITests/AnimationWindowStateScrubUI (плеер состояния, реальный
  EditAsset с собственным плеером + пиксельная проверка, строки треков слоёв),
  GameUITests/WordFallProto.FlyingLetterScrubWithoutSceneUpdateFadesLayers (клон
  реального прототипа, скраб без апдейта сцены, кадры 20_scrub_*.png).

## 2026-08-26 (22) — Прыжки траектории при скраббинге

- `FlightTrajectoryComponent` перебрасывал случайное смещение в коридоре сплайна
  каждый раз, когда `position` возвращался в 0 — при скраббинге в редакторе
  курсор постоянно проходит через старт, и объект прыгал между разными дугами
  (воспроизведено тестом: после прохода через 0 середина траектории 130 → 39).
  Неявный переброс убран: смещение меняется только явным `ResetRandomOffset()`;
  FxView вызывает его перед каждым полётом. Тесты o2SystemTests/FlightTrajectory
  (явный переброс варьирует точку; возврат в 0 не меняет), 
  o2EditorUITests/AnimationWindowStateScrubUI.ScrubbingIsStableWithPropertiesRefresh
  (скраб туда-обратно с Refresh панели свойств — та же точка в то же время).
  Доки components.md ru+en.

## 2026-08-26 (23) — Миксеры AnimationComponent перебивали превью в редакторе

- По стеку из отладчика: в edit-режиме `SceneEditScreen::Update` →
  `Scene::CheckChangedObjects` вызывает `Update(0)` у изменённых акторов, а
  `Widget::Update` → `AnimationComponent::OnUpdate` → миксеры пишут значения
  плеера состояния (время 0) поверх плеера окна анимации → `SetPosition(0)`,
  сдвиг офсетов лэйаута снова помечает виджет изменённым — и так каждый кадр:
  объект прыгал между курсором и стартом.
- `IAnimationState::IsInEditMode()` (`AnimationState` отдаёт `mInEditMode`,
  выставляемый `BeginPreview`), миксеры `ITrackMixer::IsPreviewedInEditor()`;
  `OnUpdate` пропускает миксеры, чьи треки принадлежат превьюируемому состоянию.
  Тесты o2SystemTests/AnimationComponentEditMode (Update(0) не перебивает
  значение внешнего плеера, после EndPreview — снова ведёт состояние),
  UI-тест скраба дополнен `OnObjectChanged` + `CheckChangedObjects` (без фикса
  падает: position 0 вместо 0.81).

## 2026-08-26 (24) — Окно анимации: линии треков при скролле, частицы при скраббинге

- Рендер-тест окна анимации (o2EditorUITests/AnimationWindowScrollUI, кадры в
  build/o2/TestScreenshots/anim_scroll_*.png) показал две проблемы пула
  контролов треков: (1) `AnimationTreeNode::FreeTrackControl` снимает контрол
  через `RemoveChild(…, false)` — без события он оставался в реестре отрисовки
  старого узла (`mChildrenInheritedDepth`) и с его `mParentWidget`, а узел,
  переиспользованный для другой строки, продолжал рисовать чужие линии;
  (2) контрол из кэша пристыковывался к узлу, пока тот ещё отвязан от дерева,
  и получал `mResEnabledInHierarchy = false` — строки без ключей и кривых.
  Фиксы: `Actor::RemoveChild` без события всё равно перерегистрирует drawable
  (`OnDrawbleParentChanged`), `Widget::OnChildRemoved` сбрасывает `mParentWidget`
  ребёнка; `Tree::CreateVisibleNodeWidget` после подключения узла обновляет
  флаги детей (`UpdateResEnabledInHierarchy`). Тесты o2SystemTests/WidgetRemoveChild
  и UI-тест скролла (каждая видимая строка: один включённый контрол, в реестре
  отрисовки только реальные дети).
- Частицы саб-трека при скраббинге оставались на старом месте: кадры
  запекались в `SetTime` саб-трека до обновления трансформов, т.е. по
  трансформу до применения value-треков того же клипа. `ParticlesEmitterComponent::SetTime`
  в edit-режиме редактора сначала обновляет трансформ корня актора.
  UI-тест SubTrackParticlesFollowMovedActor (без фикса частицы на −32 при акторе на 356).

## 2026-08-26 (25) — Кадры частиц пекутся по положению актора в момент кадра

- Перегенерация «в текущей точке» делала world-space частицы относительными:
  весь кэш пересчитывался там, где эмиттер стоит сейчас. Теперь при запекании
  кадра i саб-трек просит родительский плеер вычислить value-треки клипа на
  время кадра (`ParticlesEmitter::editorBakeFrameEvaluator`, ставится в
  `AnimationSubTrack::Player::SetTarget`; `AnimationPlayer::EvaluateValueTracks`
  вычисляет только value-треки, не саб-треки), а `ParticlesEmitterComponent::OnEditorBakeFrame`
  обновляет трансформ корня актора. Эмиттер стоит там, где клип ставит актор в
  этот момент: world-space частицы остаются вдоль пути, relative едут с
  актором (BasisChanged). Кадр, не совпадающий с текущим трансформом (правка
  траектории), сбрасывает кэш; зерно `seed + индекс кадра` сохраняет рисунок.
  В play-режиме редактора evaluator не используется (поведение как в игре).
  `BasisChanged` защищён от вырожденного базиса нулевого размера.
  Тесты o2SystemTests/ParticlesEmitterScrub (JumpScrubBakesFramesAlongThePath,
  RelativeParticlesTravelWithTheActorOnScrub, MovedEmitterRebakesFramesInPlace,
  RebakeKeepsParticlePattern), UI-тест SubTrackParticlesFollowMovedActor
  (world-space: шлейф от старта до актора, при скрабе назад — до актора в тот момент).
- Длина саб-трака эмиттера не менялась при правке emission duration:
  `IAnimation::onDurationChanged` (эмиттер шлёт из `UpdateDuration`), саб-трек
  подписывается в `SetTarget` и обновляет `mSubTrackDuration` сразу (плюс
  проверка в `Evaluate`). Тест EmitterDurationChangeResizesSubTrack.
  Доки Editor/Animation/animation.md ru+en.

## 2026-08-26 (26) — Крэши в MemoryManager: причина и фиксы

- Стек: `operator delete` из dispatch-блока → `MemoryManager::OnMemoryRelease`
  → падение в `std::map::find` (порченый узел). Сам менеджер потокобезопасен:
  все три метода под `recursive_mutex`, узлы map выделяются не через нашу
  перегрузку (`operator new(size_t)` не переопределён — только вариант с
  файлом/строкой, `nm` подтверждает), вложенный `OnMemoryRelease` при
  освобождении узла попадает в уже отвязанное дерево. Стресс-тест
  o2UtilTests/MemoryManagerThreads (8 потоков × 20k mmalloc/mfree/mnew/delete)
  проходит. Значит map — жертва порчи кучи извне.
- Источник порчи: сегодняшняя подписка `AnimationSubTrack::Player` на
  `IAnimation::onDurationChanged` через сырой `mTarget` — деструктор плеера
  отписывался у уже уничтоженного эмиттера (перезагрузка сцены при входе/выходе
  из play: акторы умирают, а плеер окна анимации живёт до следующего EditAsset)
  → запись в освобождённую память. Плюс давний риск `mTarget->SetTime` по
  мёртвому таргету в `Evaluate`. Фикс: `WeakRef<IAnimation> mTargetLink`,
  отписка/эвалюация только пока таргет жив. Тест
  o2SystemTests/ParticlesEmitterScrub.SubTrackPlayerAndEmitterOutliveEachOtherSafely
  (оба порядка смерти).
- Попутно замечено (не правил): в тестовом окружении актор с ребёнком или
  компонентом после `Destroy()` + кадры остаётся жив (1 лишняя сильная ссылка),
  предшествует сегодняшним правкам; в редакторе не воспроизводится напрямую.

## 2026-08-26 (27) — Поворот/масштаб виджетов; плавная рамка при зуме

- Виджеты рисовались и раскладывались по осевому `GetWorldRect()`: поворот и
  масштаб (уже посчитанные в `mWorldTransform` ActorTransform) никуда не шли.
  Введено разделение: лэйаут живёт в «лэйаут-пространстве» (осевой прямоугольник,
  как раньше — ничего не меняется для неповёрнутых виджетов), а мировой базис —
  прямоугольник с поворотом/масштабом виджета и родителей. `WidgetLayout::
  GetLayoutToWorldBasis` / `WorldToLayoutPoint` / `IsWorldTransformPlain`;
  `WidgetLayer::UpdateLayout` ставит drawable по базису (`SetBasis`) когда
  трансформ не плоский, иначе `SetRect` как раньше; хит-тест слоя переводит точку
  в лэйаут-пространство; `Widget::UpdateBounds` берёт AABB повёрнутого следа.
  Дочерние виджеты наследуют поворот/масштаб через `mParentTransform`. Поворот —
  вокруг `pivot` (у WidgetLayout по умолчанию (0,0)). Ограничение: отсечение
  `ScrollArea` остаётся осевым. Тесты o2RenderTests/WidgetTransform
  (плоский без изменений, поворот слоёв, масштаб, наследование ребёнком,
  хит-тест по повёрнутой форме). Доки ui.md ru+en.
- «Порог» рамки при зуме — это округление лэйаута сценических виджетов до целых
  единиц (`FloorRectangle`): при 10× нужно тянуть 5+ px, чтобы рект изменился.
  `WidgetLayout::SetSceneLayoutsRounding`: редактор выключает округление в режиме
  правки (`OnStarted`/`SetPlaying`), в play и в игре — как раньше; UI самого
  редактора округляется всегда (не на сцене). Порог «клик vs перетаскивание» в
  FrameTool теперь в экранных пикселях. Тест SceneLayoutsRoundingSwitch.

## 2026-08-26 (28) — Крэш в редакторе анимации: висячие делегаты плееров треков

- Крэш в `AnimationTrack<float>::Player::Evaluate` (стек через
  `UpdateSubTrackDuration` → `onKeysChanged` → `AnimationWindow::OnAnimationChanged`)
  плавал от раскладки памяти; воспроизведён UI-тестом (правка emission duration
  при открытом окне анимации) и пойман ASan-сборкой (`build-asan`): heap-use-after-free.
- Корень (давний): `IAnimationTrack::IPlayer::SetTrack` подписывался на
  `onKeysChanged` трека лямбдой с захватом `this` и `Ref` трека — лямбду нельзя
  отписать, и каждый уничтоженный плеер (перепривязка `ReattachAnimationStates`
  → `BindTracks` → `mTrackPlayers.Clear()`, смена клипа, окно анимации) оставлял
  в треке делегат на освобождённую память; заодно `Ref` трека в его же событии
  давал цикл и утечку треков. Мой `UpdateSubTrackDuration` стал чаще дёргать
  `onKeysChanged` и вскрыл это. Фикс: `WeakRef<IAnimationTrack> mBoundTrack`,
  делегат-метод `OnTrackKeysChanged` (`THIS_FUNC`), отписка в `SetTrack` и
  деструкторе. Тесты o2SystemTests/AnimationPlayerRebind (перепривязка +
  изменение ключей; трек освобождается вместе с клипом).
- `Function::Invoke` теперь рассылает по снимку списка и пропускает делегаты,
  удалённые во время рассылки (раньше `back()` на опустевшем списке и вызов
  удалённого делегата). Тесты o2UtilTests/Function (самоотписка, отписка
  соседа). `SearchKey` защищён от индекса кэша вне диапазона.

## 2026-08-26 (29) — Параметры эффектов частиц применяются сразу

- Правка градиента/кривых эффекта на инстансе прототипа не применялась до
  скраббинга: `ParticlesEmitter::AddEffect` не ставил эффекту `mEmitter`
  (его ставил только `OnEffectsListChanged` после десериализации), а
  конструкторы копирования эффектов были компиляторными — градиент/кривая
  **разделялись** между прототипом и клоном, и их `onKeysChanged` (лямбда с
  `this` исходного эффекта) инвалидировал кадры прототипа, а не инстанса.
  Параметры самого эмиттера (`SetParticlesPerSecond` и т.п.) зовут `OnChanged`
  напрямую — потому применялись сразу. Фиксы: `AddEffect`/`RemoveEffect`
  ставят/снимают `mEmitter`; у эффектов с градиентами/кривыми явные
  конструкторы копирования с глубоким клонированием и деструкторы; подписка на
  `onKeysChanged` — отписываемым делегатом (`ListenKeysChanged`, идемпотентно
  при десериализации) вместо лямбд с `this` (та же семья висячих делегатов).
  Нюанс: `mmake` для типов без конструктора с `RefCounter*` ставит счётчик
  **после** конструирования, так что `Ref(this)` внутри конструктора
  копирования эмиттера (через `AddEffect`) падал на нулевом счётчике —
  `AddEffect` ставит ссылку только при наличии счётчика, а клоны привязывают
  эффекты и shape в `ParticlesEmitter::PostRefConstruct` (хук `RefMaker`).
  Тесты o2SystemTests/ParticlesEffects: AddEffectLinksTheEmitter,
  CloneDeepCopiesCurvesAndLinksItsOwnEmitter, DestroyedEffectLeavesNoDelegateOnItsGradient,
  GradientEditRebakesParticlesImmediately. Полный ctest 356/356.

## 2026-08-28 (30) — Медленное перетаскивание хендлов рамки

- `DragHandle::OnCursorStillDown` отбрасывал кадры с `cursor.delta < 0.5 px`, а
  дельта в `Input` — накопленная за кадр: при медленном движении (меньше полпикселя
  за кадр) отбрасывался каждый кадр, и хендл (рамка `FrameTool`) не двигался вообще.
  Теперь сравнивается расстояние от последней обработанной позиции курсора
  (`mLastDragCursorPos`), так что дробные дельты накапливаются; неподвижный курсор
  по-прежнему не генерирует `onChangedPos`.
  Тест o2EditorUITests/DragHandleSlowDragUI.SubPixelFrameDeltasAccumulate.

## 2026-08-28 (31) — Правки Burst (поздний саб-трек) применяются без скраба

- Воспроизвести по коду не удалось: хедлесс-тест с саб-треком от 0.5 с
  (ParticlesEmitterScrub.LateSubTrackEmitterAppliesEditsWithoutScrub), UI-тест с
  выбранным Burst и включённым вьюером компонента
  (AnimationWindowStateScrubUI.LateSubTrackEmitterEditAppliesWhileSelected) и тест на
  реальном прототипе через стейт-плеер (GameUITests WordFallProto.FlyingLetterLateBurst
  EditAppliesWithoutScrub) — все зелёные: правка применяется на позиции скраба.
- Единственная найденная асимметрия: `InvalidateBakedFrames` не перезапекал кадр при
  `mPlaying == true`, а у саб-контролируемого эмиттера флаг залипает (play/replay в
  заголовке компонента, `RewindAndPlay`): сам он никогда не доходит до конца, так
  что `mPlaying` не сбрасывается, и любая правка ждала скраба или play-режима
  (там сцена перезагружается). Sparks, у которого в фабрике `Stop()`, этому не
  подвержен. Теперь при `mSubControlled` кадр перезапекается независимо от флага.
  Тест ParticlesEmitterScrub.PlayingFlagDoesNotBlockEditsOfSubControlledEmitter
  (падал до правки).

## 2026-08-28 (32) — Ревью o2: развязка саб-трека и частиц, сборка, перезапекания

- Саб-трек больше не знает о частицах: вместо `dynamic_cast<ParticlesEmitter*>` под
  `#if IS_EDITOR` в `AnimationSubTrack::Player::SetTarget` — общий хук `IAnimation::
  SetSubControlEvaluator/GetSubControlEvaluator/EvaluateSubControlOwner`. Плеер ставит
  цели `THIS_FUNC(EvaluateOwnerAt)` (value-треки владельца на `beginTime + localTime`),
  в `ReleaseTarget` (деструктор, смена цели) снимает его, только если он ещё его —
  цель могли перебиндить. `ParticlesEmitter::OnEditorBakeFrame` зовёт
  `EvaluateSubControlOwner`. `UpdateSubTrackDuration` в `Evaluate` убран — есть подписка.
- Сборка без редактора: `OnEditorBakeFrame` (определение в .cpp и override в компоненте,
  meta) под `#if IS_EDITOR`, как и объявление. `IsPreviewedInEditor` в
  `AnimationComponent::OnUpdate` — тоже под `IS_EDITOR` (виртуальный обход по всем трекам
  каждый кадр в игровой сборке был бесполезен).
- Перезапекания: (а) `mIsBaking` — `InvalidateBakedFrames` игнорируется, пока кадры
  пекутся/восстанавливаются (value-трек на свойстве эмиттера дёргал `OnChanged` на каждом
  кадре и чистил `mBakedFrames` изнутри `CheckBakedFrames`); (б) сравнение трансформа
  кадра с эмиттером, поставленным на время **самого кадра** (`idx/60`), а не на
  неокруглённое `mTime` — движущийся актор сбрасывал кэш почти на каждом скрабе;
  (в) сеттеры эмиттера выходят при неизменённом значении — рефреш окна свойств пишет все
  значения обратно и делал N полных перезапеков за рефреш. Публичный
  `GetBakedFramesCount()` для тестов.
- `AddEffect` ставит `mEmitter` безусловно; конструктор копирования кладёт клоны в
  `mEffects` напрямую (`PostRefConstruct` линкует), без проверки счётчика.
- `Function::Invoke`: без копии вектора — счётчик глубины диспетча в `TypeData`
  (в неиспользуемом хвосте payload), удаление во время диспетча ставит tombstone
  (`EraseFunction`), компактизация по выходу из внешнего вызова; `Clear` во время
  диспетча обнуляет слоты. Обходы списка null-безопасны. Тесты: добавление делегата во
  время диспетча, рекурсивный Invoke, Clear во время диспетча.
- `WidgetLayer`: после поворота/масштаба виджета и возврата к «плоскому» трансформу
  drawable сбрасывает угол/шир (`SetRect` их не трогает) — флаг `mDrawableTransformed`,
  чтобы не трогать собственный угол слоя, которого виджет не крутил.
  `WidgetLayout::IsPointInside` учитывает мировой поворот/масштаб (в т.ч. родительский)
  через `WorldToLayoutPoint`; вырожденный базис (scale 0) — guard по детерминанту.
- `ColorGradient::Evaluate` держит крайние цвета за ключами, как `Curve` и треки.
- Комментарии-эссе ужаты до однострочных; баннер `FlightTrajectoryComponent` — 2 строки.
- Не трогал (осознанно): глобальный `srand` при запекании (нужен per-emitter RNG —
  отдельная задача), `SetSubControlled` не сбрасывается в деструкторе плеера (старое
  поведение; сброс при живом втором плеере опасен), `Text` без `SetBasis`.
